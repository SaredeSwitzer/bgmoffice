{
  "name": "bgmoffice",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"server,client\" --prefix-colors \"blue,green\" \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "start": "npm run start --prefix server",
    "build": "npm run lint && cd client && npm install && npm run build",
    "lint": "eslint client/src server api"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.122.0",
    "@simplewebauthn/server": "^13.3.2",
    "@supabase/supabase-js": "^2.50.3",
    "@vercel/functions": "^3.9.5",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^8.5.2",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.1.1",
    "pg": "^8.13.3",
    "stripe": "^22.2.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "eslint": "^9.39.5",
    "eslint-plugin-react-hooks": "^7.1.1"
  }
}
