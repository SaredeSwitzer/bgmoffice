# Never send node_modules — each stage installs its own
node_modules/
**/node_modules/

# Client source not needed for the server build
client/

# Git and local config
.git/
.env
*.db
*.db-shm
*.db-wal

# Build artefacts
client/dist
